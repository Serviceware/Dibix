using Dibix.Sdk.Abstractions;

namespace Dibix.Generators
{
    internal static class TypeNames
    {
        public static readonly string TaskAttributeTypeName = typeof(TaskAttribute).FullName;
        public static readonly string TaskPropertyAttributeTypeName = typeof(TaskPropertyAttribute).FullName;
    }
}