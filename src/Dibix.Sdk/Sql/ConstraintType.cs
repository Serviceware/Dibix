namespace Dibix.Sdk.Sql
{
    public enum ConstraintType
    {
        None,
        PrimaryKey,
        ForeignKey,
        Unique,
        Check,
        Default,
        Nullable
    }
}