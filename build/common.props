<Project>
  <Import Project="dependencies.props" />
  <Import Project="version.props" />

  <PropertyGroup>
    <Product>Dibix</Product>
    <Company>helpLine CLM AG</Company>
    <Authors>helpLine CLM AG</Authors>
    <Copyright>© helpLine CLM AG.</Copyright>
	
    <AssemblyOriginatorKeyFile>$(MSBuildThisFileDirectory)helpline.snk</AssemblyOriginatorKeyFile>
    <SignAssembly>true</SignAssembly>
    <PublicSign Condition="'$(OS)' != 'Windows_NT'">true</PublicSign>
    <VersionSuffix Condition="'$(VersionSuffix)'!='' AND '$(BuildNumber)' != ''">$(VersionSuffix)-$(BuildNumber)</VersionSuffix>
  </PropertyGroup>

</Project>
